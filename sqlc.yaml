version: "2"
sql:
  - engine: "postgresql"
    queries: "server/db/queries"
    schema: "server/db/schema.sql"
    gen:
      go:
        package: "storage"
        out: "server/internal/storage"
        sql_package: "pgx/v5"
        emit_json_tags: true
        json_tags_case_style: "camel"
